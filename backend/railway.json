{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
```

4. Commit message: `Railway Konfiguration hinzugefügt`
5. Klicke **"Commit new file"**

**✅ Checkpoint:** Dein Repository hat jetzt die Ordnerstruktur:
```
financial-chatbot/
├── backend/
│   ├── main.py
│   ├── requirements.txt
│   ├── Procfile
│   └── railway.json
└── README.md
